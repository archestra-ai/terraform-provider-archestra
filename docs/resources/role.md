---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "archestra_role Resource - archestra"
subcategory: ""
description: |-
  Manages a custom RBAC role in Archestra. Roles define sets of permissions that can be assigned to users.
---

# archestra_role (Resource)

Manages a custom RBAC role in Archestra. Roles define sets of permissions that can be assigned to users.

## Example Usage

```terraform
resource "archestra_role" "developer" {
  name        = "Developer"
  description = "Can manage agents and MCP servers"
  permissions = [
    "agents:read",
    "agents:write",
    "mcp_servers:read",
    "mcp_servers:write"
  ]
}

resource "archestra_role" "viewer" {
  name        = "Viewer"
  description = "Read-only access to agents and profiles"
  permissions = [
    "agents:read",
    "profiles:read"
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) The name of the role
- `permissions` (List of String) List of permissions in format 'resource:action' (e.g., 'agents:read', 'agents:write', 'mcp_servers:read')

### Optional

- `description` (String) Description of what this role allows

### Read-Only

- `id` (String) Role identifier
