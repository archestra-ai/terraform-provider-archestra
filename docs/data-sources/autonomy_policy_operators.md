---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "archestra_autonomy_policy_operators Data Source - archestra"
subcategory: ""
description: |-
  Operators supported by archestra_tool_invocation_policy and archestra_trusted_data_policy condition rules — pulled live from the backend so HCL stays in sync with the platform version.
---

# archestra_autonomy_policy_operators (Data Source)

Operators supported by `archestra_tool_invocation_policy` and `archestra_trusted_data_policy` condition rules — pulled live from the backend so HCL stays in sync with the platform version.

## Example Usage

```terraform
# Operators supported by the current backend version for tool-invocation
# and trusted-data policy conditions. Use to gate HCL on whether a
# specific operator (e.g. `regex`) is available before referencing it.
data "archestra_autonomy_policy_operators" "all" {}

output "supported_operators" {
  value = [for op in data.archestra_autonomy_policy_operators.all.operators : op.value]
}

# Fail the plan if the operator the policy depends on is not supported.
locals {
  operator_values = toset([for op in data.archestra_autonomy_policy_operators.all.operators : op.value])
}

check "regex_supported" {
  assert {
    condition     = contains(local.operator_values, "regex")
    error_message = "Backend does not advertise `regex` operator — upgrade the platform or drop the regex-based policy."
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Read-Only

- `operators` (Attributes List) Supported operators with their wire `value` (use this in `conditions[*].operator`) and a human-friendly `label`. (see [below for nested schema](#nestedatt--operators))

<a id="nestedatt--operators"></a>
### Nested Schema for `operators`

Read-Only:

- `label` (String) Human-readable label (e.g. `Starts With`).
- `value` (String) Operator wire identifier (e.g. `equal`, `contains`, `startsWith`).
