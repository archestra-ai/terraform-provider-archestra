---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "archestra_agent_label_values Data Source - archestra"
subcategory: ""
description: |-
  All label values currently in use across agents, optionally filtered by key. Pair with archestra_agent_label_keys to drive policy-condition input validation in HCL.
---

# archestra_agent_label_values (Data Source)

All label values currently in use across agents, optionally filtered by `key`. Pair with `archestra_agent_label_keys` to drive policy-condition input validation in HCL.

## Example Usage

```terraform
# All values seen for a specific label key — pair with the keys data
# source to validate that a policy `value` field references an
# already-used value.
data "archestra_agent_label_values" "team" {
  key = "team"
}

output "team_label_values" {
  value = data.archestra_agent_label_values.team.values
}

# Across-all-keys mode: omit `key` to get the union of every value used.
data "archestra_agent_label_values" "all" {}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `key` (String) Optional label key to scope results.

### Read-Only

- `values` (List of String) Distinct label values across agents (filtered by `key` when set).
