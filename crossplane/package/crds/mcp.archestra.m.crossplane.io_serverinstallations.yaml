---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: serverinstallations.mcp.archestra.m.crossplane.io
spec:
  group: mcp.archestra.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - archestra
    kind: ServerInstallation
    listKind: ServerInstallationList
    plural: serverinstallations
    singular: serverinstallation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ServerInstallation is the Schema for the ServerInstallations
          API. <no value>
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServerInstallationSpec defines the desired state of ServerInstallation
            properties:
              forProvider:
                properties:
                  accessTokenSecretRef:
                    description: Personal access token for the MCP server
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  agentIds:
                    description: Agent IDs to auto-assign tools to on install
                    items:
                      type: string
                    type: array
                  catalogId:
                    description: Catalog item ID (UUID of the `archestra_mcp_registry_catalog_item`
                      resource) this installation is based on.
                    type: string
                  catalogIdRef:
                    description: Reference to a RegistryCatalogItem in mcp to populate
                      catalogId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  catalogIdSelector:
                    description: Selector for a RegistryCatalogItem in mcp to populate
                      catalogId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  environmentValues:
                    additionalProperties:
                      type: string
                    description: Environment variable values for the MCP server installation
                    type: object
                    x-kubernetes-map-type: granular
                  isByosVault:
                    description: When true, environment_values and user_config_values
                      are treated as vault references
                    type: boolean
                  name:
                    description: The name of the MCP server installation.
                    type: string
                  secretId:
                    description: Secret UUID for the MCP server installation. Set
                      explicitly to reference a pre-created secret; otherwise the
                      backend creates one when `user_config_values` is set and writes
                      the resulting UUID back here.
                    type: string
                  serviceAccount:
                    description: Kubernetes service account override for the MCP server
                      pod
                    type: string
                  teamId:
                    description: Team ID for team-scoped installations
                    type: string
                  userConfigValues:
                    additionalProperties:
                      type: string
                    description: User configuration field values for the MCP server
                      installation
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  accessTokenSecretRef:
                    description: Personal access token for the MCP server
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  agentIds:
                    description: Agent IDs to auto-assign tools to on install
                    items:
                      type: string
                    type: array
                  catalogId:
                    description: Catalog item ID (UUID of the `archestra_mcp_registry_catalog_item`
                      resource) this installation is based on.
                    type: string
                  catalogIdRef:
                    description: Reference to a RegistryCatalogItem in mcp to populate
                      catalogId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  catalogIdSelector:
                    description: Selector for a RegistryCatalogItem in mcp to populate
                      catalogId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  environmentValues:
                    additionalProperties:
                      type: string
                    description: Environment variable values for the MCP server installation
                    type: object
                    x-kubernetes-map-type: granular
                  isByosVault:
                    description: When true, environment_values and user_config_values
                      are treated as vault references
                    type: boolean
                  name:
                    description: The name of the MCP server installation.
                    type: string
                  secretId:
                    description: Secret UUID for the MCP server installation. Set
                      explicitly to reference a pre-created secret; otherwise the
                      backend creates one when `user_config_values` is set and writes
                      the resulting UUID back here.
                    type: string
                  serviceAccount:
                    description: Kubernetes service account override for the MCP server
                      pod
                    type: string
                  teamId:
                    description: Team ID for team-scoped installations
                    type: string
                  userConfigValues:
                    additionalProperties:
                      type: string
                    description: User configuration field values for the MCP server
                      installation
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: ServerInstallationStatus defines the observed state of ServerInstallation.
            properties:
              atProvider:
                properties:
                  agentIds:
                    description: Agent IDs to auto-assign tools to on install
                    items:
                      type: string
                    type: array
                  catalogId:
                    description: Catalog item ID (UUID of the `archestra_mcp_registry_catalog_item`
                      resource) this installation is based on.
                    type: string
                  displayName:
                    description: The actual name of the MCP server installation as
                      returned by the API. The API may append a suffix to ensure uniqueness.
                    type: string
                  environmentValues:
                    additionalProperties:
                      type: string
                    description: Environment variable values for the MCP server installation
                    type: object
                    x-kubernetes-map-type: granular
                  id:
                    type: string
                  isByosVault:
                    description: When true, environment_values and user_config_values
                      are treated as vault references
                    type: boolean
                  name:
                    description: The name of the MCP server installation.
                    type: string
                  secretId:
                    description: Secret UUID for the MCP server installation. Set
                      explicitly to reference a pre-created secret; otherwise the
                      backend creates one when `user_config_values` is set and writes
                      the resulting UUID back here.
                    type: string
                  serviceAccount:
                    description: Kubernetes service account override for the MCP server
                      pod
                    type: string
                  teamId:
                    description: Team ID for team-scoped installations
                    type: string
                  toolIdByName:
                    additionalProperties:
                      type: string
                    description: Lookup table from each tool's wire name (`<server>__<short>`)
                      to its bare tool UUID — same data as `tools[*].id` but keyed
                      for one-line lookups. Null while tools are still being discovered.
                    type: object
                    x-kubernetes-map-type: granular
                  tools:
                    items:
                      properties:
                        assignedAgentCount:
                          description: Number of agents this tool is currently assigned
                            to. Quick visibility without fetching the full assignment
                            list.
                          type: number
                        assignedAgents:
                          items:
                            properties:
                              id:
                                description: Agent UUID.
                                type: string
                              name:
                                description: Agent name (the agent's `name` field
                                  on `archestra_agent` / `archestra_llm_proxy` / `archestra_mcp_gateway`).
                                type: string
                            type: object
                          type: array
                        createdAt:
                          description: RFC 3339 timestamp of when the tool was first
                            registered with the backend.
                          type: string
                        description:
                          description: Human-readable description as advertised by
                            the MCP server. May be null.
                          type: string
                        id:
                          description: Tool UUID. Use as `tool_id` on `archestra_tool_invocation_policy`
                            / `archestra_trusted_data_policy`.
                          type: string
                        name:
                          description: Tool name (the MCP server's own identifier
                            — stable across installs of the same catalog item).
                          type: string
                        parameters:
                          description: JSON Schema for the tool's input parameters,
                            encoded as a JSON string. Use `jsondecode(t.parameters)`
                            to introspect required fields, types, etc. for validation
                            or downstream codegen.
                          type: string
                      type: object
                    type: array
                  userConfigValues:
                    additionalProperties:
                      type: string
                    description: User configuration field values for the MCP server
                      installation
                    type: object
                    x-kubernetes-map-type: granular
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
