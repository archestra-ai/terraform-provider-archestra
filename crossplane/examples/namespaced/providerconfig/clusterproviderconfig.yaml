apiVersion: archestra.m.crossplane.io/v1beta1
kind: ClusterProviderConfig
metadata:
  name: default
spec:
  credentials:
    source: Secret
    secretRef:
      name: archestra-creds
      namespace: crossplane-system
      key: credentials
