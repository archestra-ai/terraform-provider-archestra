Example usage of archestra_sso_provider

This folder contains an example Terraform configuration for the `archestra_sso_provider` resource.

See `main.tf` for a runnable example (requires a running Archestra API and credentials).